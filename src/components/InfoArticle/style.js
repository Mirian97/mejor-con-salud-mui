import styled from '@emotion/styled'

export const CustomInfoArticle = styled('div')`
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-bottom: 20px;

  .container-chip {
    .item-chip {
      cursor: pointer;
    }
  }
`
